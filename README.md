Nodejs version : v24.11.0
To install latest angular version open cmd and type : npm install -g @angular/cli@latest
    Angular version : v20.3.9
How to clone the angular project : 
    Step-by-step setup instructions:
        1- Git clone https://github.com/mileddd/frontend-ideas.git
        2- After cloning the repository, checkout to the master branch : git checkout master
        3- Install the dependecies : npm install
        4- Use the environment.example.ts to create your own environment.ts
        4- ng serve to run the app and the app will run on localhost:4200 by default
        5- In order to login : username : "admin" , password: "123456"

    List of completed bonus features : 
        1- Include login page
        2- Add interceptor to handle http client
        3- Add loader for api requests
        4- Block multiple votes per user
        5- Add profile section